{
  "name": "fotosafe-app-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "npm test && node scripts/validate-site.js",
    "test:browser": "node scripts/browser-localization-qa.js"
  }
}
